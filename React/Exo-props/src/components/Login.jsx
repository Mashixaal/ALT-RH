import React from "react";

const Login = ({onClick}) => {
    return(
        <>
            <button onClick={onClick}>Log in</button>
            <p>Bienvenue</p>
        </>
    )
}
export default Login;
